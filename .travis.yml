language: python

python:
- 3.8.6

env:
  global:
    - PIPENV_VENV_IN_PROJECT=1
    - PIPENV_IGNORE_VIRTUALENVS=1

install:
- pip install -q pipenv
- pipenv sync --dev

script:
- pipenv run flake8

branches:
  only:
  - master

after_success:
- pipenv run codecov
